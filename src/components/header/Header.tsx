import React from 'react'
import styled from 'styled-components'
import { StyledHeader } from './HeaderStyle'

const Header = () => {
    return <StyledHeader>Memo game</StyledHeader>
}

export default Header
